# Прод/прямой доступ к API (без прокси Vite). Пример: http://127.0.0.1:8000
# Оставь пустым в dev — запросы идут на тот же origin, Vite проксирует на Laravel.
VITE_API_BASE_URL=

# Только dev: куда проксировать /vacancies и /test (см. vite.config.js)
VITE_DEV_API_PROXY=http://127.0.0.1:8000
